# Renders

Esta carpeta almacena los videos finales (`.mp4`) exportados exitosamente por el Motor de Renderizado de la aplicación.

Por defecto, los archivos llevarán nombres como `clase_{class_id}_final.mp4`.

Esta carpeta es ignorada por el repositorio Git (excepto este archivo `notas.md`) para evitar subir videos pesados generados localmente.
